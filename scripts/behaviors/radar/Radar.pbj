"pbj-0.0.3"

package Radar.Protocol;

// Track messages are sent by new objects to every other object
message Track {
    required string type = 1; // object's type, e.g. lemming, box etc.
}

// Container message
message Container {
    optional Track track = 1;
}